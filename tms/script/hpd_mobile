#!/bin/bash
#
# This daemon polls the common.replog table and sends one HPD message
# for each record of the table. The record will be deleted immediately
# after it is sent
#

DAEMON=HPD.HPDOnline
INSTANCE=Mobile
DATABASES="common mobile ordercanal"
cd $(dirname $0)/..
. script/daemon.sh
